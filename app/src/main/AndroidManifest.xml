<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dipaocean"
        android:usesCleartextTraffic="true">

        <!-- SPLASH SCREEN JADI HALAMAN AWAL -->
        <activity
            android:name=".Pertemuan_6.splash.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Pertemuan_6.login.LoginActivity" />
        <activity android:name=".Pertemuan_6.login.RegisterActivity" />
        <activity android:name=".Pertemuan_7.main.MainActivity" />
        <activity android:name=".Pertemuan_6.main.MainActivity" />
        <activity android:name=".Pertemuan_6.main.WebViewActivity" />
        <activity android:name=".Pertemuan_4.welcome.WelcomeActivity" />
        <activity android:name=".Pertemuan_11.onboarding.OnBoardingActivity" />
        <activity android:name=".Pertemuan_11.note.NoteFormActivity" />
        <activity android:name=".Pertemuan_7.home.pertemuan_13.ThirteenthActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".Pertemuan_11.note.NotificationReceiver"
            android:exported="false" />

    </application>

</manifest>